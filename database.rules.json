{
    "rules": {
        "presence": {
            "$uid": {
                ".read": true,
                ".write": "newData.hasChildren(['name', 'color', 'lastSeen'])",
                "lastSeen": {
                    ".validate": "newData.isNumber()"
                }
            }
        },
        "invites": {
            "$uid": {
                ".read": true,
                "$senderUid": {
                    ".write": true,
                    ".validate": "newData.hasChildren(['game', 'mode', 'status'])"
                }
            }
        },
        "lobby": {
            "chat": {
                ".read": true,
                "$msgId": {
                    ".write": "newData.hasChildren(['content', 'uid', 'timestamp'])",
                    ".validate": "newData.child('content').isString() && newData.child('uid').isString()"
                }
            }
        },
        "games": {
            "$gameId": {
                ".read": true,
                ".write": true,
                "global": {
                    "pileColors": {
                        ".write": true
                    },
                    "piecePositions": {
                        ".write": true
                    }
                },
                "chat": {
                    ".read": true,
                    "$chatId": {
                        ".write": true,
                        ".validate": "newData.hasChildren(['content', 'uid'])"
                    }
                },
                "playerData": {
                    "$uid": {
                        ".write": "(!data.exists() && (!root.child('games').child($gameId).child('playerData').exists() || root.child('games').child($gameId).child('playerData').numChildren() < 2)) || data.exists()"
                    }
                },
                "users": {
                    "$uid": {
                        ".write": "(!data.exists() && (!root.child('games').child($gameId).child('users').exists() || root.child('games').child($gameId).child('users').numChildren() < 2)) || data.exists()"
                    }
                }
            }
        },
        "gameData": {
            "$gameId": {
                ".read": true,
                "events": {
                    "$eventId": {
                        ".write": "!data.exists()",
                        ".validate": "newData.hasChildren(['type', 'payload', 'uid', 'timestamp'])"
                    }
                }
            }
        }
    }
}
