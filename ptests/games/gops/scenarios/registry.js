/**
 * Re-export platform scenario vocabulary. Add game-specific slices here only when needed.
 * @see ptests/shared/scenarios/registry.js
 */
module.exports = require('../../../shared/scenarios/registry');
