/**
 * Bananagrams MP test constants — re-exported from mp-ctx (single source of truth).
 */
const {
    HOST_UID,
    GUEST_UID,
    BUNCH,
    BANANA_2P_PLAYERS,
    EXPECTED_MP_2P_POOL
} = require('./mp-ctx');

module.exports = {
    HOST_UID,
    GUEST_UID,
    BUNCH,
    BANANA_2P_PLAYERS,
    EXPECTED_MP_2P_POOL
};
