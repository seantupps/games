# New game scaffold

Copy this folder to `games/<your-id>/` and follow **`docs/GAME_PLATFORM.md`** in order.

Do not add `if (gameName === '<id>')` in `engine.js`. Set capabilities in `shared/games/registry.js` instead.
