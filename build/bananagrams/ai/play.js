#!/usr/bin/env node
/** Same as ptests/shared/bananagrams_ai/play.js — run from repo via build path. */
const { main } = require('../../../ptests/shared/bananagrams_ai/play.js');
process.exit(main());
